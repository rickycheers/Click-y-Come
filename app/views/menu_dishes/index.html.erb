<h1>Listing menu_dishes</h1>

<table>
  <tr>
    <th>Menu</th>
    <th>Dish</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @menu_dishes.each do |menu_dish| %>
  <tr>
    <td><%= menu_dish.menu_id %></td>
    <td><%= menu_dish.dish_id %></td>
    <td><%= link_to 'Show', menu_dish %></td>
    <td><%= link_to 'Edit', edit_menu_dish_path(menu_dish) %></td>
    <td><%= link_to 'Destroy', menu_dish, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Menu dish', new_menu_dish_path %>
