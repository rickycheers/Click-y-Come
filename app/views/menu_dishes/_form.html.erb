<%= form_for(@menu_dish) do |f| %>
  <% if @menu_dish.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_dish.errors.count, "error") %> prohibited this menu_dish from being saved:</h2>

      <ul>
      <% @menu_dish.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :menu_id %><br />
    <%= f.text_field :menu_id %>
  </div>
  <div class="field">
    <%= f.label :dish_id %><br />
    <%= f.text_field :dish_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
