<%= form_for(@office) do |f| %>
  <% if @office.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@office.errors.count, "error") %> prohibited this office from being saved:</h2>

      <ul>
      <% @office.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <% if current_user.phone.nil? %>
  <%= fields_for current_user do |uf| %>
  <div class="field">
	Teléfono<br />
	<%= uf.text_field :phone %>
  </div>
  <% end %>
  <% end %>

  <div class="field">
    Nombre de la Empresa:<br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    Dirección:<br />
    <%= f.text_field :address %>
  </div>
  <div class="field">
    Ciudad:<br />
    <%= f.text_field :city %>
  </div>
  <div class="field">
    Estado:<br />
    <%= f.text_field :state %>
  </div>
  <div class="field">
    Código Postal:<br />
    <%= f.text_field :zip %>
  </div>
  <div class="field">
    Referencias para llegar a la oficina:<br />
    <%= f.text_area :references %>
  </div>
  <div class="field">
    <%= f.hidden_field :geo %>
  </div>
  <div class="actions">
    <input type="submit" name="commit" value="Enviar registro" >
  </div>
<% end %>
